import { PageShell, PlaceholderBody } from "@/components/page-shell"

export default function ClientPortfolioPage() {
  return (
    <PageShell
      title="Client Portfolio"
      description="One row per client — health at a glance"
    >
      <PlaceholderBody what="Client portfolio view (v_client_portfolio)" />
    </PageShell>
  )
}
